import SectionTitle from "../../Shared/SectionTitle/SectionTitle";


const Form = () => {
    return (
        <div>
            <SectionTitle
            subHeading={''}
            heading={''}
            ></SectionTitle>
        </div>
    );
};

export default Form;